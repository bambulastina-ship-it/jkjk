# Hashed filenames change whenever content changes, so they can be cached hard.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# The entry document must always be revalidated so deploys take effect at once.
/index.html
  Cache-Control: public, max-age=0, must-revalidate
