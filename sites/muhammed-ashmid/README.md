# Agent 3 — Muhammed Ashmid — INCOMPLETE, ON HOLD

Status: paused at the client's instruction. Do not resume without being asked.

What is here is a PARTIAL build from the abandoned WebGL/shader direction. The
subagent writing it was cut off mid-file by a session rate limit, so the HTML,
CSS and JS are unfinished and were never rendered or verified. Do not assume any
of it works.

The three.js bundle has been deleted: the revised scope rules out WebGL, shaders
and 3D entirely.

## When this resumes, rebuild to the current scope, not this code
See sites/katerina-telesh/ for the pattern that was accepted:
  - roughly 3 full-screen scrolls, no more
  - scroll 1 agent/hero (portrait leads, face first, one large obvious CTA)
  - scroll 2 property showcase
  - scroll 3 featured/closing CTA
  - plain HTML/CSS/JS, ~15KB, no libraries, no build step
  - premium via typography, spacing, composition and restraint

## Worth keeping from this attempt
- fonts/ — Fraunces + Plus Jakarta Sans, self-hosted woff2. A good pairing for
  Ashmid and distinct from Lara (Bodoni Moda/Archivo) and Katerina (Sora/Inter
  Tight). Google Fonts is blocked in this environment, so self-hosting is required.
- img/ — his two supplied images, already copied in.

## Still outstanding from the client
- BRN number (legally required on UAE property marketing; never invent one)
- WhatsApp / phone
- Year started
- More property photography — only two images exist, so no gallery
